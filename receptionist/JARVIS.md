Personality now lives in the vault boot file, not here.

First Talk run seeds `~/.jarvis/vault/BOOT.md` from `memory/templates/BOOT.md`.
Edit that file (keep it small). Speech rules and “no tools” stay in Talk code.
